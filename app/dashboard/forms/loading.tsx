import { Typography } from "@mui/material";
import React from "react";

export default function Loading() {
  return (
    <React.Fragment>
      <Typography
        sx={{
          fontSize: "10px",
        }}
      >
        Loading
      </Typography>
    </React.Fragment>
  );
}
